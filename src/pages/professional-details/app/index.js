import { navigateTo } from '@/utils';
import { get } from 'lodash';

// Mock queue data

export default {
    name: 'ProfessionalCollectionApp',
    data() {
        return {
            
        };
    },
    mounted() {
    },
    methods: {
    }
};
